[variables]
    NIXPACKS_PHP_VERSION = '8.4'

[phases.install]
    cmds = ['composer install --no-dev --optimize-autoloader', 'npm install --legacy-peer-deps']

[phases.build]
    cmds = ['npm run build']